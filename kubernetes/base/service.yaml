---
apiVersion: v1
kind: Service
metadata:
  name: gitops-demo
  namespace: gitops-demo
  labels:
    app: gitops-demo
    managed-by: kustomize
spec:
  type: ClusterIP
  selector:
    app: gitops-demo
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP